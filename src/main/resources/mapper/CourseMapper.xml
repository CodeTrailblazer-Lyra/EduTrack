<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.lukix.edutrack.repository.CourseRepository">

    <resultMap id="CourseResultMap" type="xyz.lukix.edutrack.entity.Course">
        <id property="id" column="id"/>
        <result property="lessonCode" column="lesson_code"/>
        <result property="name" column="name"/>
        <result property="credit" column="credit"/>
        <result property="teacherId" column="teacher_id"/>
    </resultMap>

    <select id="findAll" resultMap="CourseResultMap">
        SELECT * FROM course WHERE deleted = 0
    </select>

    <select id="findById" resultMap="CourseResultMap" parameterType="long">
        SELECT * FROM course WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" parameterType="xyz.lukix.edutrack.entity.Course" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course(lesson_code, name, credit, teacher_id) VALUES(#{lessonCode}, #{name}, #{credit}, #{teacherId})
    </insert>

    <update id="update" parameterType="xyz.lukix.edutrack.entity.Course">
        UPDATE course SET lesson_code = #{lessonCode}, name = #{name}, credit = #{credit}, teacher_id = #{teacherId} WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById" parameterType="long">
        UPDATE course SET deleted = 1 WHERE id = #{id}
    </update>

    <select id="existsByLessonCode" resultType="boolean" parameterType="string">
        SELECT COUNT(*) > 0 FROM course WHERE lesson_code = #{lessonCode} AND deleted = 0
    </select>

    <select id="existsByLessonCodeAndIdNot" resultType="boolean">
        SELECT COUNT(*) > 0 FROM course WHERE lesson_code = #{lessonCode} AND id != #{id} AND deleted = 0
    </select>

    <select id="findByLessonCode" resultMap="CourseResultMap" parameterType="string">
        SELECT * FROM course WHERE lesson_code = #{lessonCode} AND deleted = 0
    </select>

    <select id="existsById" resultType="boolean" parameterType="long">
        SELECT COUNT(*) > 0 FROM course WHERE id = #{id} AND deleted = 0
    </select>

</mapper>